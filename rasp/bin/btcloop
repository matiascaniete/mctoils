#!/usr/bin/env bash

frame() {
	echo "$1"
	sleep .25
}

animation() {
	frame "-"
	frame "--"
	frame "---"
	frame "----"
	frame "-----"
	frame "------"
	frame "-------"
	frame "--------"
}

while :
do
	animation
	BTC=$(btc)
	printf "%8s" "$BTC"
	sleep 5m
done;
